services:
  claims-control-tower:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: claims-control-tower
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      PYTHONPATH: /app
    restart: unless-stopped
